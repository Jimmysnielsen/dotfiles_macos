- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.tmux.conf: tmux.conf
    ~/.zshrc: zshrc

- create:
    - ~/downloads
    - ~/work

#  - shell:
#   - [git submodule update --init --recursive, Installing submodules]

