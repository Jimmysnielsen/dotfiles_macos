- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.tmux.conf: tmux.conf
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig

- create:
    - ~/downloads
    - ~/work

- shell:
  - [git submodule update --init --recursive, Installing submodules]


